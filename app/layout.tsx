// ============================================================
// Vision Glass & Interior — Root Layout
// ============================================================

import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';
import { Providers } from '@/components/providers';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'Vision Glass & Interiors — Employee Expense Management',
  description:
    'Track, manage, and report employee expenses with real-time dashboards, automated reports, and complete audit trails.',
  keywords: ['expense management', 'employee expenses', 'Vision Glass & Interiors', 'financial tracking'],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
